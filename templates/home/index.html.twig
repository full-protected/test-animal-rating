{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1>Notez votre animal préféré</h1>
    {{ form_start(form) }}
    {{ form_row(form.userName) }}
    {{ form_row(form.animalName) }}
    {{ form_row(form.score) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
